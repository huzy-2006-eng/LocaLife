VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# Optional. Powers the real AI Concierge (query -> structured filters) via
# Groq's free tier. Without this, the concierge falls back to a local
# keyword parser -- get a free key at console.groq.com (API Keys).
VITE_GROQ_API_KEY=your-groq-api-key
